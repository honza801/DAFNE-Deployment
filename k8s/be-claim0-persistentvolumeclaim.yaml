apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: be-claim0
  name: be-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
